import ResourcePage from "@/components/resources/ResourcePage";
import { BALANCE } from "@/config/resources/personalSessions";

const Balance = () => <ResourcePage content={BALANCE} />;

export default Balance;
