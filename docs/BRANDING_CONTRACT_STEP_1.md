# Formula branding contract - Step 1

Canonical ownership is the versioned contract at:

`/Users/standardmacbook/Formula-build-backups/branding-alignment-2026-09-12/BRAND_CONTRACT.md`

The landing site remains the expressive, black-first marketing variant. Step 1 makes the shared Formula orange/blue role names explicit and corrects action foreground mappings to contrast-safe ink pairs in `src/index.css`.

The existing marketing highlight orange `hsl(20 100% 63%)` (`#FF8C42`) remains a deliberate landing-only variant. The canonical workspace/native orange is `#FF6A1A`. Black backgrounds, ticket-pill CTAs, gradients and page composition remain unchanged in this step.

Typography, broad component migration, naming, navigation and store work remain pending later sessions.
